{
  "name": "webchat",
  "version": "1.0.0",
  "description": "Fast anonymous web chat",
  "private": true,
  "scripts": {
    "dev": "pnpm --parallel --stream dev",
    "build": "pnpm --recursive --stream build",
    "test": "pnpm --recursive --stream test",
    "lint": "pnpm --recursive --stream lint",
    "clean": "pnpm --recursive --stream clean"
  },
  "keywords": [
    "chat",
    "websocket",
    "fastify",
    "react",
    "typescript"
  ],
  "author": "",
  "license": "MIT",
  "devDependencies": {
    "@types/node": "^20.10.0",
    "typescript": "^5.3.3"
  },
  "engines": {
    "node": ">=18.0.0",
    "pnpm": ">=8.0.0"
  }
}
